name: Feature Summary Generator

on:
  issues:
    types: [labeled]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  generate-feature-summary:
    name: Generate Feature Summary
    # Only run if the label "accepted" was just added and it's a feature request or enhancement
    if: |
      github.event.label.name == 'accepted' &&
      (contains(github.event.issue.labels.*.name, 'enhancement') ||
       contains(github.event.issue.title, '[Feature]') ||
       contains(github.event.issue.title, '[Enhancement]'))
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
            
      - name: Install Copilot CLI
        run: npm install -g @github/copilot

      - name: Execute Prompt
        env:
          # The CLI looks for this token to authenticate headlessly
          COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          # Construct the prompt with issue details
          # Use a file to avoid command line length limits or escaping issues
          cat <<EOF > prompt.txt
          Please summarize the intentions of the following issue:
          
          Title: $ISSUE_TITLE
          
          Body:
          $ISSUE_BODY
          EOF
          
          # Use -p for prompt, -s for silent (output only the answer)
          # Read prompt from file to ensure safe handling
          copilot -s -p "$(cat prompt.txt)"